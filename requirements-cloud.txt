# ── HTTP / API ─────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Orquestração ───────────────────────────────────────────────────────────────
dagster>=1.7.0
dagster-webserver>=1.7.0

# ── Transformação (Silver) ─────────────────────────────────────────────────────
pandas>=2.0.0

# ── MongoDB (Gold + MCP) ───────────────────────────────────────────────────────
pymongo>=4.6.0

# ── MCP Server ─────────────────────────────────────────────────────────────────
fastmcp==1.0.0

# ── Interface Streamlit + LLM ──────────────────────────────────────────────────
streamlit>=1.35.0
openai>=1.30.0

# ── Utilitários ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# Nota: pins do Windows (greenlet, griffe, starlette) foram removidos.
# Este arquivo é exclusivo para o deploy no Dagster Cloud (Linux).
# Para rodar localmente no Windows, use requirements.txt.
